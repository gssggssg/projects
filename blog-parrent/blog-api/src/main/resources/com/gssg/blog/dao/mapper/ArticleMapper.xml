<?xml version="1.0" encoding="UTF-8" ?>
<!--MyBatis配置文件-->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gssg.blog.dao.mapper.ArticleMapper">

    <select id="listArchives" resultType="com.gssg.blog.dao.dos.Archives">
        SELECT FROM_UNIXTIME(create_date / 1000, '%y') AS year, FROM_UNIXTIME(create_date / 1000, '%m') AS MONTH, COUNT(*) AS count FROM ms_article GROUP BY year, month
    </select>

</mapper>